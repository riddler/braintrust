---
description: Promote a backlog item to a GitHub issue.
---

Invoke the `github-projects` skill to promote an item:

```
Skill(skill: "github-projects")
```

Context: Promote a backlog item to a GitHub issue. $ARGUMENTS

If no specific item is mentioned, list the current draft items in Backlog status and let the user select one.
